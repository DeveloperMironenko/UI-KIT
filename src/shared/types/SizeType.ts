export type SizeType = "s" | "m" | "l";

export type WidthType = `${number}px` | `${number}%` | `${number}vw`;

export type HeightType = `${number}px` | `${number}%` | `${number}vh`;

export type SpaceType = `${number}px` | `${number}%`;
